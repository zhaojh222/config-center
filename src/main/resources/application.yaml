server:
  servlet:
    context-path: /configcenter
  port: 8080
spring:
  application:
    name: config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/zhaojh222/config-server